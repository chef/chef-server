BEGIN;

\set schema quote_ident('public')

SELECT no_plan();

SELECT opscode.all_table_permissions(:schema, 'heimdall', ARRAY['SELECT', 'UPDATE', 'INSERT', 'DELETE']);
SELECT opscode.all_table_permissions(:schema, 'heimdall_ro', ARRAY['SELECT']);

SELECT opscode.all_view_permissions('debug', 'heimdall', ARRAY['SELECT']);
SELECT opscode.all_view_permissions('debug', 'heimdall_ro', ARRAY['SELECT']);

SELECT * FROM finish();
ROLLBACK;
