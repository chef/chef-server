{
  "type" : "object",
  "required" : true,
  "properties" : {
    "name" :
      { "type" : "string",
        "description" : "Change the current name to a unique namespace matching the format [a-z0-9\\-_]+",
        "required" : false },
    "validator" :
      { "type" : "boolean",
        "description" : "Change whether or not the client is a validator.",
        "required" : false }
  }
}
