#!/bin/sh

<% 
  app_dir = "#{@options['install_dir']}/current/#{@options['app_path']}" 
%>

cd <%= app_dir %>
exec 2>&1

exec chpst -u<%= @options['user'] %> /usr/bin/env\
  PATH=/usr/local/bin:$PATH\
  bundle exec unicorn -E production -c /etc/unicorn/oc-id.rb config.ru

