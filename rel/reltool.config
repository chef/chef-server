{sys, [
       {lib_dirs, ["../apps", "../deps"]},
       {rel, "chef_api", "0.1",
        [
         kernel,
         stdlib,
         sasl,
         crypto,
         mochiweb,
         webmachine,
         ejson,
         oauth,
         ibrowse,
         couchbeam,
         chef_common,
         chef_rest
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "chef_api"},
       {profile, embedded},
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},
       {excl_archive_filters, [".*"]},
       {app, sasl, [{incl_cond, include}]},
       {app, crypto, [{incl_cond, include}]},
       {app, mochiweb, [{incl_cond, include}]},
       {app, webmachine, [{incl_cond, include}]},
       {app, ejson, [{incl_cond, include}]},
       {app, oauth, [{incl_cond, include}]},
       {app, ibrowse, [{incl_cond, include}]},
       {app, couchbeam, [{incl_cond, include}]},
       {app, chef_common, [{incl_cond, include}]},
       {app, chef_rest, [{incl_cond, include}]}
      ]}.

{target_dir, "chef_api"}.
{overlay_vars, "vars.config"}.

{overlay, [
           {mkdir, "log/sasl"},
           {mkdir, "etc/keys"},
           {copy, "../../opscode-chef/chef-server-webui/lib/webui_priv.pem", "etc/keys/webui_priv.pem"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/chef_api", "bin/chef_api"},
           {template, "files/app.config", "etc/app.config"},
           {copy, "files/vm.args", "etc/vm.args"}
          ]}.
